# ============================================================
# VoxIntel Python NLP Service Dependencies
# Install with: pip install -r python_services/requirements.txt
# Then: python -m spacy download en_core_web_sm
# ============================================================

flask==3.0.0
flask-cors==4.0.0

# Production WSGI server — Flask's built-in app.run() dev server isn't meant
# for production traffic. Render (and similar PaaS) should start this with
# `gunicorn --bind 0.0.0.0:$PORT app:app`, not `python app.py`.
gunicorn==21.2.0

# NLP core libraries
nltk==3.8.1
spacy==3.7.2

# Sentence embeddings for semantic evaluation
# NOTE: sentence-transformers==2.2.2 (the old pin) calls huggingface_hub's
# deprecated cached_download(), which was removed in huggingface_hub>=0.20 —
# pip resolves the newest huggingface_hub by default since it wasn't pinned,
# so 2.2.2 would silently fail to import and evaluation fell back to crude
# word-overlap similarity. 5.x is compatible with current huggingface_hub/transformers.
sentence-transformers==5.6.0

# ML utilities
scikit-learn==1.3.2
numpy==1.26.2

# PDF and DOCX parsing
pdfminer.six==20221105
python-docx==1.1.0

# Speech processing (optional — gracefully skipped if unavailable)
SpeechRecognition==3.10.1
pydub==0.25.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
