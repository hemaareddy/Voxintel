# ============================================================
# VoxIntel Python NLP Service Environment Variables
# Copy this file to .env and fill in your values (optional —
# sensible defaults are used if these are unset).
# ============================================================

# Host/port the Flask service binds to
FLASK_HOST=0.0.0.0
FLASK_PORT=5001
